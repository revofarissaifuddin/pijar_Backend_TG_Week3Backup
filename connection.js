/* // create connection database
const { Pool } = require("pg");
const pool = new Pool({
    user: 'Asus',
    host: 'localhost',
    database: 'pijar_backend_tg_week3backup',
    password: "",
    port: 5432,
});

module.exports = pool; */